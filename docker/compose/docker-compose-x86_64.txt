services:
  ros2:
    image: isaac_ros_dev-x86_64
    container_name: aerostack2_container
    environment:
      DISPLAY: $DISPLAY
      XAUTHORITY: $XAUTHORITY
      SSH_AUTH_SOCK: $SSH_AUTH_SOCK
    runtime: nvidia
    privileged: true
    restart: always
    volumes:
      # display related 
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ~/.Xauthority:/root/.Xauthority
      # ssh related
      # - $SSH_AUTH_SOCK:$SSH_AUTH_SOCK
      # git
      - ~/.gitconfig:/root/.gitconfig_local
      # working dirs
      - ~/ultralytics:/root/ultralytics
      - ~/minerva_training:/root/arquimea
      - ~/minerva_training/datasets:/root/datasets
      - ~/format_unifier:/root/format_unifier
      - /mnt/a/rodrigo:/root/runs
      - /mnt/a/rodrigo/arquimea:/root/arquimea/arquimea
    network_mode: "host"
    command: tail -f /dev/null
